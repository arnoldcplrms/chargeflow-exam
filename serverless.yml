service: chargeflow
frameworkVersion: '3'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x

functions:
  api:
    handler: microservices/products/handler.handler
    events:
      - http:
          path: /products/{any+}
          method: get
          cors: true

      
